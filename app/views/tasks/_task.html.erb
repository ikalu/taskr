  <nav>
    <%= task.body %>
  </nav>
  <%= button_to "Edit", edit_task_path(task), method: :get %>

  <%= button_to "Done", task, method: :delete, class: "delete-task", data: { task: "#{task.id}" }%>
